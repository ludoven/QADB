{
  "name": "qadb-desktop-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "docs:dev": "vitepress dev website --host 127.0.0.1",
    "docs:build": "vitepress build website",
    "docs:preview": "vitepress preview website --host 127.0.0.1"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
